# Task 4 Fix Plan — Add Missing `Company.taxId` Raw-String Test Coverage

## Deviation

Task 4 implementation missed `Company.taxId` in raw-string test coverage. The field is confirmed encrypted (`company.entity.ts` L26 `EncryptedValue | string | null`) but was not exercised in either:
- `src/__tests__/dtos.test.ts` compile-time `satisfies CreateCompanyDto` block
- `src/__tests__/entities/company-and-client.test.ts` runtime Company test

## Fix Steps

1. Update `src/__tests__/dtos.test.ts`:
   - In the `CreateCompanyDto` `satisfies` block, add `taxId: '30-50012345-6'`.
   - Add `expect(dto.taxId).toBe('30-50012345-6');`.

2. Update `src/__tests__/entities/company-and-client.test.ts`:
   - In the Company `satisfies` block, add `taxId: '30-50012345-6'`.
   - Add `expect(company.taxId).toBe('30-50012345-6');`.

3. Run `npm test` to confirm the new assertions pass.
4. Commit with message: `test: add missing Company.taxId raw-string coverage`.
