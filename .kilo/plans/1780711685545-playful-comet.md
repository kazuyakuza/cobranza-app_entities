# Global Plan — Encryption & Data Model Improvements

**TODO File**: `.agent/todos/20260605/20260605-todo-1.md`
**Pattern B**: Each `##` section is a task (6 tasks total).
**Date**: 2026-06-06
**Status**: Approved by user ("Approve Global and Task Plans")

---

## Critical Workflow Steps

### Step 2: Git Feature Branch Setup → implementer
- Commit unstaged changes; checkout `main`; create `feat/encryption-and-data-model`.

### Step 3: Version Update → implementer
- Bump patch version in `package.json` (0.0.1 → 0.0.2); commit.

---

## Task 1: Encryption Infrastructure (`src/types/encrypted.ts` + docs)
### 4.1 Analysis & Planning → architect
### 4.2 Implementation → implementer
### 4.3 Code Review → code-reviewer (+ fix cycle via implementer)
### 4.4 Documentation → docs-specialist
### 4.5 Verification → implementer
### 4.6 Task Completion → implementer

---

## Task 2: Common Types & Structures (`src/types/location.ts` + entity renames)
### 4.1 Analysis & Planning → architect
### 4.2 Implementation → implementer
### 4.3 Code Review → code-reviewer (+ fix cycle via implementer)
### 4.4 Documentation → docs-specialist
### 4.5 Verification → implementer
### 4.6 Task Completion → implementer

---

## Task 3: Entity Updates — Encryption
### 4.1 Analysis & Planning → architect
### 4.2 Implementation → implementer
### 4.3 Code Review → code-reviewer (+ fix cycle via implementer)
### 4.4 Documentation → docs-specialist
### 4.5 Verification → implementer
### 4.6 Task Completion → implementer

---

## Task 4: DTO Adjustments
### 4.1 Analysis & Planning → architect
### 4.2 Implementation → implementer
### 4.3 Code Review → code-reviewer (+ fix cycle via implementer)
### 4.4 Documentation → docs-specialist
### 4.5 Verification → implementer
### 4.6 Task Completion → implementer

---

## Task 5: Documentation & Guidelines
### 4.1 Analysis & Planning → architect
### 4.2 Implementation → implementer
### 4.3 Code Review → code-reviewer (+ fix cycle via implementer)
### 4.4 Documentation → docs-specialist
### 4.5 Verification → implementer
### 4.6 Task Completion → implementer

---

## Task 6: Update CSV File
### 4.1 Analysis & Planning → architect
### 4.2 Implementation → implementer
### 4.3 Code Review → code-reviewer (+ fix cycle via implementer)
### 4.4 Documentation → docs-specialist
### 4.5 Verification → implementer
### 4.6 Task Completion → implementer

---

## Step 5: TODO File Completion → implementer
- Rename TODO file with `-DONE` suffix; merge feature branch to `main`; push to origin.

---

## Design Decisions (from user)

1. **DTOs**: Use `EncryptedValue` type in DTOs (derive via `Omit` from entity as currently done).
2. **JSON Schemas**: Update all 20+ schema files too.
3. **Hash columns scope** (only these encrypted fields get hashes):
   - Company: `taxIdHash`
   - Client: `fullNameHash`, `taxIdHash`
   - BankTransaction: `referenceHash`, `descriptionHash`
   - Notification: `toHash`

---

## High-Level Implementation Summary

### Task 1 — `src/types/encrypted.ts` + docs
- New file: `src/types/encrypted.ts` — `EncryptedValue` interface with JSDoc
- New file: `docs/security-encryption-policy.md` — encryption flow, key mgmt, rules
- Update: `src/types/index.ts` — export `EncryptedValue`

### Task 2 — `src/types/location.ts` + entity renames
- New file: `src/types/location.ts` — `Location` + `Address` interfaces
- Update: `src/types/index.ts` — export `Location`, `Address`
- Update: `src/entities/company/company.entity.ts` — rename `address` → `location: Location`
- Update: `src/entities/client/client.entity.ts` — rename `address` → `location: Location`

### Task 3 — Entity field encryption
Entities to modify (add `EncryptedValue`, hash columns):
| Entity | Fields to encrypt | Hash columns |
|---|---|---|
| **Company** | `businessName`, `taxId`, `contact`, `phone`, `location` | `taxIdHash` |
| **User** | `fullName`, `phone` | — |
| **Client** | `fullName`, `taxId`, `email`, `phone`, `location` | `fullNameHash`, `taxIdHash` |
| **BankTransaction** | `reference`, `description` | `referenceHash`, `descriptionHash` |
| **BankStatement** | `notes` | — |
| **Notification** | `to`, `from`, `subject`, `body` | `toHash` |
| **PaymentProof** | `notes` (encrypt) | — |

### Task 4 — DTO updates
- Update all affected `.dto.ts` files using existing `Omit` pattern.
- Ensure types consistent with entity changes from Tasks 2 and 3.

### Task 5 — Documentation
- Update `README.md` with encryption section.
- Create `docs/encryption-examples.md` with encrypt/decrypt code examples.

### Task 6 — CSV update
- Update `.agent/project-info/entities-definition.csv`:
  - Rename `address` → `location`, type → `JSONB`
  - Change encrypted field types from `String` → `JSONB`
  - Add hash columns to affected entities
  - Add JSON format comments for encrypted fields

---

## JSON Schema Updates (cross-cutting, handled within relevant tasks)

All `.schema.json` files under `src/schemas/` must reflect:
- Encrypted fields as `{ "type": "object", "properties": { "encryptedData": {...}, "keyName": {...}, ... } }`
- Hash column additions
- `address` → `location` rename
- `Location` type as nested object with `address` sub-object
