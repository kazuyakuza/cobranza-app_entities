# Task 2 Fix Plan — Entity BaseEntity Migration

## Issue Found

Stale `SoftDeletable` references remain in JSDoc comments inside DTO files under `src/entities/`. The `SoftDeletable` abstraction was removed as part of the BaseEntity migration; audit/soft-delete fields now live directly in `BaseEntity`. Keeping these references is misleading and inconsistent with the current model.

## Affected Files

1. `src/entities/receipt/receipt-template.dto.ts` (line 5)
2. `src/entities/invoice/invoice-template.dto.ts` (line 5)
3. `src/entities/debt/debt-schedule.dto.ts` (line 5)

## Required Changes

### `src/entities/receipt/receipt-template.dto.ts`

Update the JSDoc comment on line 5:

```text
 * Omits `BaseEntity` audit fields.
```

### `src/entities/invoice/invoice-template.dto.ts`

Update the JSDoc comment on line 5:

```text
 * Omits `BaseEntity` audit fields.
```

### `src/entities/debt/debt-schedule.dto.ts`

Update the JSDoc comment on line 5:

```text
 * Omits `BaseEntity` and system-managed fields.
```

## Verification After Fix

- Run `npm run typecheck` — must pass with 0 errors.
- Run `npm run test` — must pass.
- Run `npm run lint` — must pass.
- Search `src/entities/` for `SoftDeletable` — must return 0 matches.

## Notes

- No runtime behavior changes; this is a documentation-only cleanup.
- The Omit type field lists are already correct and do not need modification.
