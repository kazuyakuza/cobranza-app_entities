# Documentation Updates — Post-DTO Review

## Context

After completing the DTO/Entity Field Origin Review (v0.5.0), the documentation needs further alignment and expansion:

1. `docs/usage-nestjs.md` — Needs TypeORM entity example and NATS+JetStream microservice patterns.
2. `docs/usage-angular.md` — Still shows hand-rolled `Omit<Debt, ...>` instead of the broad `Create*Dto` + narrowing pattern. Needs class-transformer/class-validator frontend examples.
3. `README.md` — Needs a table of contents/index at the top (file is long) and a full consistency review.

## Tasks

### 1 — Update `docs/usage-nestjs.md` [DONE]

1. Add a **TypeORM Entity Example** section (or expand §5) showing a complete `DebtEntity` class with decorators implementing the library `Debt` interface.
2. Add a **NATS + JetStream Microservices** section showing:
   - A NestJS microservice configured with NATS transport
   - A producer/publisher sending events using library DTOs
   - A consumer/ subscriber receiving events and validating payloads with `class-validator`
   - The DTO class `implements` the narrowed library DTO alias

### 2 — Update `docs/usage-angular.md` [DONE]

1. Replace hand-rolled `Omit<Debt, 'id' | 'createdAt' | ... | 'debtCode'>` examples with the broad-DTO pattern:
   - Use `CreateDebtDto` from the library
   - Show narrowing with `Omit<CreateDebtDto, 'debtCode' | 'status'>` where the API should not allow those fields
2. Replace `Omit<Client, 'id' | 'createdAt' | ... | 'clientCode'>` with `Omit<CreateClientDto, 'clientCode'>`
3. Add **class-transformer + class-validator in Angular** section showing how to validate/transform API payloads on the frontend using these libraries with library DTOs

### 3 — Update `README.md`

1. Add a **Table of Contents** at the top linking to all major sections.
2. Full consistency review:
   - Ensure all DTO examples use the broad-DTO + narrowing pattern
   - Ensure encryption section mentions `EncryptedValue | string | null`
   - Ensure no outdated `name` field references in examples
   - Fix any broken or inconsistent links
3. Verify cross-references between README and `docs/*.md` files are accurate.

### 4 — Verification

1. Run `npm run build` and `npm test` to ensure no source regressions.
2. Spell-check / link-check docs if tooling is available.
3. Confirm all code snippets in docs are syntactically valid TypeScript.
