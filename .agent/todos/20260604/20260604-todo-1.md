# TODO - Cobranza App Entities Library

## Project Status

**Phase**: Initial Setup & Entity Definition  
**Goal**: Create a clean, reusable TypeScript entities library to be used across NestJS microservices and Angular frontends.

## Tasks

### 1. Project Setup [DONE]

- [x] Configure strict TypeScript settings (if not already done)
- [x] Add ESLint + Prettier (if not already done)

### 2. Core Types & Base Interfaces [DONE]

- [x] Create `src/types/common.ts` (UUID, Decimal, DateString, Money, etc.)
- [x] Create `src/interfaces/base-entity.interface.ts` (common audit fields)
- [x] Create `src/enums/index.ts` barrel (if not exists)

### 3. Enums Definition [DONE]

Important: enums must be string type, not numeric.

- [x] `DebtStatus`
- [x] `PaymentAttemptStatus`
- [x] `PaymentStatus`
- [x] `BankStatementStatus`
- [x] `BankTransactionStatus`
- [x] `NotificationType`
- [x] `NotificationChannel`
- [x] `NotificationStatus`
- [x] `Frequency` (for DebtSchedule)
- [x] `CalculationType`
- [x] `Currency`
- [x] `MatchType` (`AUTOMATIC` / `MANUAL`)
- [x] All other required enums (if I skipped any)

### 4. Entities Implementation [DONE]

Create full TypeScript interfaces for each entity:

#### Core & Multi-Tenancy

- [x] `Company`
- [x] `CompanyPlan`
- [x] `User`
- [x] `Role`
- [x] `CompanyUser`

#### Clients & Debts

- [x] `Client`
- [x] `Debt`
- [x] `DebtSchedule`

#### Invoicing

- [x] `Invoice`
- [x] `InvoiceTemplate`
- [x] `Receipt`
- [x] `ReceiptTemplate`

#### Payments & Reconciliation

- [x] `PaymentProof`
- [x] `PaymentAttempt`
- [x] `Payment`
- [x] `BankStatement`
- [x] `BankTransaction`
- [x] `PaymentMatch`

#### Communication & Summaries

- [x] `Notification`
- [x] `NotificationTemplate`
- [x] `ClientDebtSummary`
- [x] `CompanyMonthlySummary`

#### Note

If I skipped any entity, implement it too.

### 5. Organization & Exports [DONE]

- [x] Create proper folder structure per domain (if not already)
- [x] Create/Update barrel exports (`index.ts`) in every folder
- [x] Create/Update root barrel export (`src/index.ts`)
- [x] Add detailed JSDoc documentation for all properties and entities

### 6. Documentation [DONE]

- [x] Update `README.md` with installation and usage examples
- [x] Create `CHANGELOG.md`
- [x] Create `CONTRIBUTING.md` (guidelines for extending entities). Must be usable by AI agents.

### 7. Quality & Testing [DONE]

- [x] Add basic tests for enums and type compatibility
- [x] Run full type checking
- [x] Ensure no circular dependencies

### 8. Extra

- [ ] Add documentation example usage in NestJS and Angular:
  Higher-level documentation showing real usage:
  - How to import and use the entity in a Controller
  - How to use it in an Angular Service
  - Common patterns (DTOs, forms, etc.)
- [ ] Add OpenAPI / Swagger compatible decorators examples:
  Concrete code examples showing how to extend the plain interfaces with NestJS decorators.
  Focused on technical implementation (@ApiProperty(), @ApiResponse(), etc.).

## Notes

- All entities must be **plain interfaces** (no decorators) so they can be easily extended in NestJS (`@Entity()`) and Angular.
- Keep the library **framework agnostic**.
- Use consistent naming and structure based on the CSV definition and data model brief.

**Target**: Have a fully functional, importable entities library ready for use in backend and frontend projects.
